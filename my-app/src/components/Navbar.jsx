import React from "react"
import {AppBar, Typography} from '@mui/material';

const Navbar = () => {
    return (
        <AppBar><Typography>Test</Typography></AppBar>

    )
}

export default Navbar
